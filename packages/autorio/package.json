{
  "name": "autorio.ts",
  "version": "0.1.0",
  "description": "An automation mod for Factorio written in TypeScript.",
  "author": "LemonNekoGH",
  "license": "MIT",
  "keywords": [
    "factorio",
    "automation",
    "mod",
    "typescript"
  ],
  "scripts": {
    "build": "tstl && node -e \"require('node:fs').copyFileSync('data.lua','dist/data.lua')\"",
    "dev": "mkdir -p dist && cp -r info.json dist && cp data.lua dist/data.lua && tstl -p tsconfig.dev.json --watch",
    "typecheck": "tstl --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^6.1.0",
    "@proj-airi/tstl-plugin-reload-factorio-mod": "workspace:*",
    "eslint": "^9.38.0",
    "lua-types": "^2.13.1",
    "typed-factorio": "^3.33.0",
    "typescript": "^5.9.3",
    "typescript-to-lua": "^1.32.0",
    "vitest": "^4.0.4"
  }
}
