{
  "name": "autorio.ts",
  "version": "0.1.0",
  "description": "A heavily modified AIRI Factorio fork of the original autorio mod, focused on standalone NPC automation.",
  "author": "TTLouis (fork; original autorio by LemonNekoGH)",
  "license": "MIT",
  "keywords": [
    "factorio",
    "automation",
    "mod",
    "typescript"
  ],
  "scripts": {
    "build": "tstl && node -e \"const fs=require('node:fs'); fs.copyFileSync('info.json','dist/info.json'); fs.copyFileSync('data.lua','dist/data.lua'); fs.copyFileSync('LICENSE','dist/LICENSE')\"",
    "dev": "mkdir -p dist && cp -r info.json dist && cp data.lua dist/data.lua && cp LICENSE dist/LICENSE && tstl -p tsconfig.dev.json --watch",
    "typecheck": "tstl --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^6.1.0",
    "@proj-airi/tstl-plugin-reload-factorio-mod": "workspace:*",
    "eslint": "^9.38.0",
    "lua-types": "^2.13.1",
    "typed-factorio": "^3.33.0",
    "typescript": "^5.9.3",
    "typescript-to-lua": "^1.32.0",
    "vitest": "^4.0.4"
  }
}
